import string
from random import *
name = str(input("Enter Some text FOr Your password:"))
ascii_characters = input("Enter Charcters: ")
numbers = int(input("Enter Numbers"))
characters = name + ascii_characters + str(numbers)
password = "".join(choice(characters) for x in range(randint(8, 16)))
print (password)
